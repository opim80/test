-- Targeted Spy Wrapper
-- Only intercepts high-value exploit functions to avoid making 11k files from obfuscator loops

-- Cleanup old intercepted files in workspace
pcall(function()
    if listfiles and delfile then
        for _, file in pairs(listfiles("")) do
            if file:find("INTERCEPTED_LOADSTRING") then
                delfile(file)
            end
        end
    end
end)

local HttpService = game:GetService("HttpService")

local function write_log(name, content)
    -- Use a single file and append to it, or use unique names with a limit
    local filename = "spy_log_" .. name .. ".txt"
    if isfile(filename) then
        appendfile(filename, "\n\n--- NEW INTERCEPT ---\n\n" .. tostring(content))
    else
        writefile(filename, tostring(content))
    end
end

-- 1. Hook queue_on_teleport
if queue_on_teleport then
    local old_queue
    old_queue = hookfunction(queue_on_teleport, function(code)
        print("[SPY] Intercepted queue_on_teleport!")
        write_log("queue_on_teleport", code)
        
        -- We inject this exact script into the queued code so the spy persists!
        local my_source = [[
            print("[SPY] SUCCESSFULLY LOADED INTO RAID SERVER! The Spy is active.")
            local function write_log(name, content)
                local filename = "spy_log_" .. name .. ".txt"
                if isfile(filename) then appendfile(filename, "\n\n--- NEW INTERCEPT ---\n\n" .. tostring(content)) else writefile(filename, tostring(content)) end
            end
            if queue_on_teleport then
                local old_queue
                old_queue = hookfunction(queue_on_teleport, function(c)
                    write_log("queue_on_teleport_raid", c)
                    return old_queue(c)
                end)
            end
            if request then
                local old_request
                old_request = hookfunction(request, function(req)
                    write_log("requests_raid", game:GetService("HttpService"):JSONEncode(req))
                    return old_request(req)
                end)
            end
            
            -- Hook VirtualInputManager and VirtualUser
            local vim = game:GetService("VirtualInputManager")
            local vu = game:GetService("VirtualUser")
            
            local old_namecall
            old_namecall = hookmetamethod(game, "__namecall", function(self, ...)
                local method = getnamecallmethod()
                
                -- Catch Inputs
                if self == vim or self == vu then
                    if method:find("KeyEvent") or method:find("MouseButton") or method:find("Click") or method:find("Button") then
                        local args = {...}
                        local arg_strings = {}
                        for i,v in pairs(args) do table.insert(arg_strings, tostring(v)) end
                        local log_str = string.format("INPUT:%s:%s(%s)", tostring(self), method, table.concat(arg_strings, ", "))
                        write_log("combat_logic", log_str)
                    end
                end
                
                -- Catch RemoteEvents & RemoteFunctions (The real combat logic!)
                if method == "FireServer" or method == "InvokeServer" then
                    if tostring(self) == "tick" then
                        return old_namecall(self, ...)
                    end
                    local args = {...}
                    local arg_strings = {}
                    for i, v in pairs(args) do 
                        if type(v) == "table" then
                            table.insert(arg_strings, "Table")
                        elseif type(v) == "buffer" then
                            local b_str = buffer.tostring(v)
                            local hex = {}
                            for j=1, #b_str do table.insert(hex, string.format("%02X", string.byte(b_str, j))) end
                            table.insert(arg_strings, "Buffer[" .. table.concat(hex, " ") .. "]")
                        else
                            table.insert(arg_strings, type(v) .. "(" .. tostring(v) .. ")")
                        end
                    end
                    local log_str = string.format("REMOTE:%s:%s(%s)", tostring(self), method, table.concat(arg_strings, ", "))
                    write_log("combat_remotes", log_str)
                end
                
                return old_namecall(self, ...)
            end)

            -- Hook Exploit-Level Input Functions (in case they bypass Roblox APIs)
            local function hook_exploit_input(func_name)
                local env = getgenv and getgenv() or getfenv(0)
                if env[func_name] then
                    local old_func
                    old_func = hookfunction(env[func_name], function(...)
                        local args = {...}
                        local arg_strings = {}
                        for i,v in pairs(args) do table.insert(arg_strings, tostring(v)) end
                        write_log("combat_logic", string.format("EXPLOIT_INPUT:%s(%s)", func_name, table.concat(arg_strings, ", ")))
                        return old_func(...)
                    end)
                end
            end
            
            local exploit_inputs = {"keypress", "keyrelease", "mouse1click", "mouse1press", "mouse1release", "mouse2click", "mouse2press", "mouse2release", "mousemoverel"}
            for _, name in ipairs(exploit_inputs) do hook_exploit_input(name) end
        ]]
        
        -- Prepend our spy to their code
        local modified_code = my_source .. "\n\n" .. code
        return old_queue(modified_code)
    end)
end

-- 2. Hook HTTP Requests (to catch payloads or work.ink checks)
if request then
    local old_request
    old_request = hookfunction(request, function(req_tbl)
        if type(req_tbl) == "table" then
            print("[SPY] Intercepted request to: " .. tostring(req_tbl.Url))
            write_log("requests", HttpService:JSONEncode(req_tbl))
        end
        return old_request(req_tbl)
    end)
end

local old_httpget
old_httpget = hookfunction(game.HttpGet, function(self, url, ...)
    print("[SPY] Intercepted HttpGet: " .. tostring(url))
    write_log("httpget", url)
    return old_httpget(self, url, ...)
end)

print("[SPY] Wrapper initialized! You can now run the obfuscated script.")
