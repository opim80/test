--This is a roblox localscript ran by the player, and has no control over the server or access to changing the server
--[[
    CSMDH meth | V1.3.1
    Professional Devil Heart Automation Utility
    
    Update Log:
    - V1.3.1:
      - Optimized checking algorithms to reduce GC overhead.
      - Added "Anti-Stuck" logic to movement (Timeout & Position check).
      - Added "View Heart" Camera toggle in Debug.
      - Added "Highlight Heart" (ESP style) in Debug.
      - Added Detailed Heart Analysis Stats (Bait Probability).
    - V1.3.0:
      - Added "Floating Heart" (AC Bait) detection using Raycast.
      - Added "Buried" detection (70% inside object).
      - Added ProximityPrompt auto-hold logic.
      - Added Inventory Check & "Tween on Max Hearts" configuration.
]]

-- [[ PRE-LOAD INTERCEPTOR ]]
local function handleNativeMainMenu()
    local lp = game:GetService("Players").LocalPlayer
    if not lp then return end
    
    local playerGui = lp:FindFirstChild("PlayerGui")
    if playerGui then
        local playButton = playerGui:FindFirstChild("MainMenu") 
            and playerGui.MainMenu:FindFirstChild("Menu")
            and playerGui.MainMenu.Menu:FindFirstChild("MainMenu")
            and playerGui.MainMenu.Menu.MainMenu:FindFirstChild("PlayButton")

        if playButton and playButton:IsA("GuiButton") then
            if getconnections then
                for _, connection in pairs(getconnections(playButton.MouseButton1Click)) do
                    connection:Fire()
                end
            end
        end
    end
end

handleNativeMainMenu()

-- [[ MAIN UI INITIALIZATION ]]
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

-- Services
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ProximityPromptService = game:GetService("ProximityPromptService")
local Camera = Workspace.CurrentCamera

-- Constants
local MESH_ID_TARGET = "rbxassetid://12439777368"
local SERVER_LOG_FILE = "fluent_server_history.json"
local LocalPlayer = Players.LocalPlayer
local MIN_Y_LIMIT = 941
local AC_BAIT_Y_LIMIT = 911
local ALIGNMENT_OFFSET = 3.5 

-- Optimization Objects (Reduce GC)
local RayParamsExclude = RaycastParams.new()
RayParamsExclude.FilterType = Enum.RaycastFilterType.Exclude
RayParamsExclude.IgnoreWater = false

local RayParamsInclude = RaycastParams.new()
RayParamsInclude.FilterType = Enum.RaycastFilterType.Include

local OverlapParamsExclude = OverlapParams.new()
OverlapParamsExclude.FilterType = Enum.RaycastFilterType.Exclude

-- State Variables
local webhookURL = ""
local webhookEnabled = false
local isInitialized = false 
local activeTweens = {}
local stopRequested = false
local velocityConnection = nil
local isHopperRunning = false

-- Debug/Tracking Variables
local cachedHeartPart = nil
local isViewingHeart = false
local currentHighlight = nil

-- UI Elements
local StatusLabel = nil
local SpeedLabel = nil 
local HeightLabel = nil 
local HeartStatsLabel = nil

-- [[ HELPER FUNCTIONS ]]

local function updateStatus(text)
    if StatusLabel then
        StatusLabel:SetTitle("Status: " .. text)
    end
end

-- Cleanup Function to prevent memory leaks in movement
local function cleanupMovement()
    -- Clear Tweens
    for _, t in ipairs(activeTweens) do
        if t then 
            pcall(function() t:Cancel() end)
            t = nil 
        end
    end
    activeTweens = {}

    -- Clear Connections
    if velocityConnection then
        velocityConnection:Disconnect()
        velocityConnection = nil
    end

    -- Reset Character Physics
    local char = LocalPlayer.Character
    if char then
        local root = char:FindFirstChild("HumanoidRootPart")
        if root then 
            root.Anchored = false 
            root.Velocity = Vector3.zero 
        end
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Landed) end
    end
end

-- Inventory Check
local function getHeartCount()
    local count = 0
    -- Check Backpack
    if LocalPlayer:FindFirstChild("Backpack") then
        for _, tool in ipairs(LocalPlayer.Backpack:GetChildren()) do
            if tool:IsA("Tool") and (string.find(string.lower(tool.Name), "heart") or string.find(string.lower(tool.Name), "devil")) then
                count = count + 1
            end
        end
    end
    -- Check Character (Equipped)
    if LocalPlayer.Character then
        for _, tool in ipairs(LocalPlayer.Character:GetChildren()) do
            if tool:IsA("Tool") and (string.find(string.lower(tool.Name), "heart") or string.find(string.lower(tool.Name), "devil")) then
                count = count + 1
            end
        end
    end
    return count
end

local function unstuckCharacter()
    local char = LocalPlayer.Character
    if not char then return end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    -- Update params with current character
    OverlapParamsExclude.FilterDescendantsInstances = {char}

    local parts = Workspace:GetPartsInPart(root, OverlapParamsExclude)
    
    if #parts > 0 then
        local attempts = 0
        while #parts > 0 and attempts < 10 do
            root.CFrame = root.CFrame + Vector3.new(0, 1, 0)
            parts = Workspace:GetPartsInPart(root, OverlapParamsExclude)
            attempts = attempts + 1
            task.wait()
        end
    end
end

local function getGroundPosition(targetX, targetZ)
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return nil end
    
    local rayOrigin = Vector3.new(targetX, char.HumanoidRootPart.Position.Y + 100, targetZ)
    local rayDirection = Vector3.new(0, -5000, 0)
    
    RayParamsExclude.FilterDescendantsInstances = {char}
    
    local raycastResult = Workspace:Raycast(rayOrigin, rayDirection, RayParamsExclude)
    
    if raycastResult then
        return Vector3.new(targetX, raycastResult.Position.Y, targetZ)
    else
        return nil
    end
end

-- Collision Group Check (Invisible Walls)
local function checkPathCollision(targetPos)
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return false end
    
    local map = workspace:FindFirstChild("map")
    local rokyo = map and map:FindFirstChild("Rokyo")
    local colGroup = rokyo and rokyo:FindFirstChild("Collision Group")
    
    if colGroup then
        local origin = char.HumanoidRootPart.Position
        local direction = targetPos - origin
        
        RayParamsInclude.FilterDescendantsInstances = {colGroup}
        
        local result = workspace:Raycast(origin, direction, RayParamsInclude)
        if result then return true end
    end
    return false
end

-- Detailed Heart Analysis (Grounding & Burial)
local function analyzeHeartPhysics(heartPart)
    local pos = heartPart.Position

    -- 1. Check if Buried (70% inside logic)
    OverlapParamsExclude.FilterDescendantsInstances = {heartPart, heartPart.Parent, LocalPlayer.Character}
    
    local partsInHeart = workspace:GetPartsInPart(heartPart, OverlapParamsExclude)
    for _, p in ipairs(partsInHeart) do
        if p.CanCollide and p.Transparency < 1 then
            return "Buried", "Heart is inside " .. p.Name
        end
    end

    -- 2. Check if Grounded (Downward Raycast)
    local rayOrigin = heartPart.Position
    local rayDir = Vector3.new(0, -3.5, 0) -- 3.5 studs down
    
    RayParamsExclude.FilterDescendantsInstances = {heartPart, heartPart.Parent, LocalPlayer.Character}

    local result = workspace:Raycast(rayOrigin, rayDir, RayParamsExclude)

    if not result then
        -- Ray hit nothing. Potential Floating Heart.
        local distFromPlayer = (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")) 
            and (LocalPlayer.Character.HumanoidRootPart.Position - pos).Magnitude or 9999
        
        if distFromPlayer < 400 then
            return "Floating", "Floating Heart (No ground detected)"
        end
    end

    return "Valid", nil
end

local function findDevilHeart()
    updateStatus("Scanning for heart...")
    local map = workspace:FindFirstChild("map")
    local rokyos = map and map:FindFirstChild("Rokyo")
    if rokyos then
        for _, object in ipairs(rokyos:GetChildren()) do
            if object:IsA("MeshPart") and object.MeshId == MESH_ID_TARGET then
                if object:FindFirstChildOfClass("PointLight") then 
                    local pos = object.Position
                    cachedHeartPart = object -- Cache for debug usage
                    
                    -- Hard Height Check
                    if pos.Y < AC_BAIT_Y_LIMIT then return "ACBait", pos, object end
                    
                    -- Physics Analysis (Floating/Buried)
                    local status, reason = analyzeHeartPhysics(object)
                    if status ~= "Valid" then
                        return status, pos, object -- Returns "Floating" or "Buried"
                    end

                    return "valid", pos, object
                end
            end
        end
    end
    cachedHeartPart = nil
    return nil, nil, nil
end

local function sendWebhook(title, message, color)
    if webhookEnabled and webhookURL ~= "" and webhookURL:find("discord.com/api/webhooks") then
        local data = {
            ["embeds"] = {{
                ["title"] = title,
                ["description"] = message,
                ["color"] = color or 0x00ff00,
                ["timestamp"] = DateTime.now():ToIsoDate()
            }}
        }
        pcall(function()
            request({
                Url = webhookURL,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = HttpService:JSONEncode(data)
            })
        end)
    end
end

local function serverHop()
    updateStatus("Preparing to hop...")
    local history = isfile(SERVER_LOG_FILE) and HttpService:JSONDecode(readfile(SERVER_LOG_FILE)) or {}
    
    history[game.JobId] = os.time()
    
    local expireMinutes = Fluent.Options.ServerLogExpiry and Fluent.Options.ServerLogExpiry.Value or 30
    local expireTime = expireMinutes * 60
    local currentTime = os.time()
    
    for id, timestamp in pairs(history) do
        if (currentTime - timestamp) > expireTime then
            history[id] = nil
        end
    end
    
    writefile(SERVER_LOG_FILE, HttpService:JSONEncode(history))

    updateStatus("Fetching server list...")
    local success, result = pcall(function()
        return HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"))
    end)

    if success and result and result.data then
        for _, server in ipairs(result.data) do
            if server.playing < server.maxPlayers and not history[server.id] and server.id ~= game.JobId then
                updateStatus("Teleporting to: " .. server.id)
                TeleportService:TeleportToPlaceInstance(game.PlaceId, server.id)
                return
            end
        end
    end
    updateStatus("No suitable servers found.")
end

-- [[ MOVEMENT ENGINE ]]

local function collectHeart(targetPart)
    if not targetPart then return end
    updateStatus("Collecting Heart...")
    
    local prompt = targetPart:FindFirstChildOfClass("ProximityPrompt")
    if not prompt then
        prompt = targetPart.Parent:FindFirstChildOfClass("ProximityPrompt")
    end

    if prompt then
        local holdTime = prompt.HoldDuration or 0
        if prompt.HoldDuration > 0 then
            prompt:InputHoldBegin()
            task.wait(holdTime + 0.1)
            prompt:InputHoldEnd()
        else
            prompt:InputHoldBegin()
            prompt:InputHoldEnd()
        end
        
        if fireproximityprompt then
            fireproximityprompt(prompt)
        end
        
        updateStatus("Heart collected.")
        Fluent:Notify({Title = "Success", Content = "Heart Collected!", Duration = 3})
    else
        updateStatus("No Prompt Found.")
    end
end

local function stopTween(finalPart)
    updateStatus("Stopping movement...")
    stopRequested = true
    cleanupMovement() 
    
    -- If we arrived at a heart, collect it
    if finalPart then
        collectHeart(finalPart)
    end
    
    updateStatus("Movement stopped.")
end

local function performTweenSegment(targetPos)
    if stopRequested then return end
    local character = LocalPlayer.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    if not rootPart then return end

    local distance = (rootPart.Position - targetPos).Magnitude
    local duration
    
    if Fluent.Options.UseSTPS.Value then
        local stps = tonumber(Fluent.Options.STPSValue.Value) or 30
        duration = distance / stps
    else
        local speed = tonumber(Fluent.Options.TweenSpeed.Value) or 0.5
        duration = (speed * distance) / 100
    end
    
    if duration <= 0 then duration = 0.1 end

    local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle[Fluent.Options.TweenEasing.Value], Enum.EasingDirection.Out)
    local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = CFrame.new(targetPos)})
    
    table.insert(activeTweens, tween)
    tween:Play()
    tween.Completed:Wait()
    
    -- Optimize Table
    for i, t in ipairs(activeTweens) do
        if t == tween then
            table.remove(activeTweens, i)
            break
        end
    end
end

local function startStagedTween(targetPos, targetObject)
    cleanupMovement() -- Safety cleanup before starting
    updateStatus("Initiating Tween...")
    stopRequested = false
    local character = LocalPlayer.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    if not rootPart then return end

    local startPos = rootPart.Position
    Fluent:Notify({
        Title = "Tween Started",
        Content = string.format("Dist: %.0f studs", (startPos - targetPos).Magnitude),
        Duration = 5
    })

    velocityConnection = RunService.Stepped:Connect(function()
        if rootPart and rootPart.Parent and not rootPart.Anchored then
            rootPart.Velocity = Vector3.new(0, -0.01, 0)
            rootPart.RotVelocity = Vector3.zero
        end
    end)
    
    if humanoid then humanoid:ChangeState(Enum.HumanoidStateType.Physics) end

    local function moveToGoal(goalPos, label)
        -- ANTI-STUCK VARIABLES
        local stuckCheckTimer = 0
        local lastStuckPos = rootPart.Position
        
        while (rootPart.Position - goalPos).Magnitude > 1 and not stopRequested do
            local currentPos = rootPart.Position
            local dist = (currentPos - goalPos).Magnitude
            
            -- Anti-Stuck Logic
            stuckCheckTimer = stuckCheckTimer + 1
            if stuckCheckTimer > 30 then -- Check every ~constant steps (depends on segment length but is a safety)
                 if (currentPos - lastStuckPos).Magnitude < 1 then
                      updateStatus("Stuck detected! Re-adjusting...")
                      rootPart.CFrame = rootPart.CFrame + Vector3.new(0,5,0) -- Pop up
                 end
                 lastStuckPos = currentPos
                 stuckCheckTimer = 0
            end

            updateStatus("Moving " .. label .. "...")
            local stepDist = tonumber(Fluent.Options.TweenDist.Value)
            local nextStep
            
            if Fluent.Options.SegmentToggle.Value and dist > stepDist then
                nextStep = currentPos + (goalPos - currentPos).Unit * stepDist
            else
                nextStep = goalPos
            end
            
            -- Logic to determine Y level
            if Fluent.Options.GroundFollow.Value then
                local groundPos = getGroundPosition(nextStep.X, nextStep.Z)
                if groundPos then
                    nextStep = Vector3.new(nextStep.X, groundPos.Y + ALIGNMENT_OFFSET, nextStep.Z)
                else
                    if nextStep.Y < MIN_Y_LIMIT then nextStep = Vector3.new(nextStep.X, MIN_Y_LIMIT, nextStep.Z) end
                end
            else
                if Fluent.Options.SplitToggle.Value then
                    if nextStep.Y < MIN_Y_LIMIT then nextStep = Vector3.new(nextStep.X, MIN_Y_LIMIT, nextStep.Z) end
                end
            end

            performTweenSegment(nextStep)
            unstuckCharacter()

            if not stopRequested and Fluent.Options.SegmentToggle.Value and (rootPart.Position - goalPos).Magnitude > 1 then 
                if not Fluent.Options.SegmentDrop.Value then
                    rootPart.Anchored = true
                    rootPart.Velocity = Vector3.zero
                end
                task.wait(Fluent.Options.TweenInterval.Value) 
                if not stopRequested and not Fluent.Options.SegmentDrop.Value then
                    rootPart.Anchored = false
                end
            end
        end
    end

    if Fluent.Options.SplitToggle.Value then
        local initialTargetY = math.max(rootPart.Position.Y, MIN_Y_LIMIT)
        if Fluent.Options.GroundFollow.Value then
            moveToGoal(Vector3.new(targetPos.X, rootPart.Position.Y, targetPos.Z), "Horizontally (Ground)")
        else
            moveToGoal(Vector3.new(targetPos.X, initialTargetY, targetPos.Z), "Horizontally")
        end
        if not stopRequested then moveToGoal(targetPos, "Vertically/Final") end
    else
        moveToGoal(targetPos, "to Target")
    end
    
    updateStatus("Reached destination.")
    stopTween(targetObject) 
end

-- [[ CORE HOPPER LOOP ]]

local function runHopperLogic()
    if isHopperRunning then return end
    isHopperRunning = true
    updateStatus("Hopper Started.")
    
    task.spawn(function()
        while Fluent.Options.HopperToggle.Value do
            local status, heartPos, heartObj = findDevilHeart()
            
            -- INVENTORY CHECK
            local currentHearts = getHeartCount()
            local maxHeartsAllowed = Fluent.Options.MaxHeartLimit.Value
            local isMaxed = currentHearts >= maxHeartsAllowed

            if status == "valid" then
                if checkPathCollision(heartPos) then
                    updateStatus("Barrier Detected! Marking as Bait.")
                    sendWebhook("⚠️ Collision Bait Detected", "Tween blocked by invisible wall. Hopping.", 0xff9900)
                    serverHop()
                elseif isMaxed and not Fluent.Options.TweenOnMax.Value then
                    updateStatus("Max hearts reached. Hopping...")
                    sendWebhook("ℹ️ Heart Found (Skipped)", "Server " .. game.JobId .. " has a heart, but " .. LocalPlayer.Name .. " is already max on hearts.", 0x00aaff)
                    serverHop()
                else
                    updateStatus("Heart found! Stopping hopper.")
                    sendWebhook("✅ Valid Heart Found!", "Target in server: " .. game.JobId .. "\nStatus: Valid", 0x00ff00)
                    if Fluent.Options.AutoTweenOnFind.Value then
                        startStagedTween(heartPos, heartObj)
                    end
                    Fluent.Options.HopperToggle:SetValue(false)
                    break
                end

            elseif status == "Floating" then
                updateStatus("Floating Heart (AC Bait) Detected!")
                serverHop()
            elseif status == "Buried" then
                updateStatus("Buried Heart (AC Bait) Detected!")
                serverHop()
            elseif status == "ACBait" then
                updateStatus("ACBait (Height) detected! Skipping...")
                serverHop()
            else
                updateStatus("No heart found.")
                serverHop()
            end
            
            updateStatus("Wait interval...")
            task.wait(10)
        end
        isHopperRunning = false
        updateStatus("Hopper Disabled.")
    end)
end

-- [[ DEBUG FEATURES ]]

local function updateDebugStats()
    if not HeartStatsLabel then return end
    
    if cachedHeartPart and cachedHeartPart.Parent then
        local heart = cachedHeartPart
        local pos = heart.Position
        
        -- Logic Checks
        local isFloating = false
        local isBuried = false
        local isColliding = checkPathCollision(pos)
        local isOnGround = false
        local prob = 0
        local checks = 0
        
        -- Floating/Buried Check
        local status, _ = analyzeHeartPhysics(heart)
        if status == "Floating" then isFloating = true end
        if status == "Buried" then isBuried = true end
        
        -- Ground Check
        local rayDir = Vector3.new(0, -3.5, 0)
        RayParamsExclude.FilterDescendantsInstances = {heart, heart.Parent, LocalPlayer.Character}
        local groundCheck = workspace:Raycast(pos, rayDir, RayParamsExclude)
        if groundCheck then isOnGround = true end
        
        -- Probability Calc
        if isFloating then prob = prob + 33 checks = checks + 1 end
        if isColliding then prob = prob + 33 checks = checks + 1 end
        if not isOnGround and not isFloating then prob = prob + 10 checks = checks + 1 end -- Suspicious if not on ground
        if isBuried then prob = prob + 100 checks = checks + 1 end
        
        if prob > 100 then prob = 100 end

        HeartStatsLabel:SetTitle(string.format(
            "Heart Stats:\nFloating: %s | Colliding: %s\nOnGround: %s | Buried: %s\nBait Probability: %d%%",
            isFloating and "✅" or "❌",
            isColliding and "✅" or "❌",
            isOnGround and "✅" or "❌",
            isBuried and "✅" or "❌",
            prob
        ))
    else
        HeartStatsLabel:SetTitle("Heart Stats: No Heart Detected")
    end
end

local function toggleHighlight()
    if not cachedHeartPart then 
        Fluent:Notify({Title = "Error", Content = "No heart cached. Scan first.", Duration = 2})
        return 
    end

    if currentHighlight then
        currentHighlight:Destroy()
        currentHighlight = nil
        Fluent:Notify({Title = "Highlight", Content = "Removed.", Duration = 1})
    else
        local hl = Instance.new("Highlight")
        hl.FillTransparency = 1
        hl.OutlineTransparency = 0
        hl.OutlineColor = Color3.fromRGB(255, 50, 50)
        hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        hl.Adornee = cachedHeartPart
        hl.Parent = cachedHeartPart
        currentHighlight = hl
        Fluent:Notify({Title = "Highlight", Content = "Added.", Duration = 1})
    end
end

local function toggleViewHeart()
    isViewingHeart = not isViewingHeart
    if isViewingHeart then
        if cachedHeartPart then
            Camera.CameraSubject = cachedHeartPart
            Fluent:Notify({Title = "Camera", Content = "Viewing Heart.", Duration = 2})
        else
            isViewingHeart = false
            Fluent:Notify({Title = "Error", Content = "No heart found to view.", Duration = 2})
        end
    else
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
            Camera.CameraSubject = LocalPlayer.Character.Humanoid
        end
        Fluent:Notify({Title = "Camera", Content = "Reset to Player.", Duration = 2})
    end
end

-- [[ UI INITIALIZATION ]]

local Window = Fluent:CreateWindow({
    Title = "CSMDH meth",
    SubTitle = "V1.3.1",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 520), 
    Acrylic = true, 
    Theme = "Dark"
})

local Tabs = {
    Automation = Window:AddTab({ Title = "Automation", Icon = "play" }),
    Debug = Window:AddTab({ Title = "Debug", Icon = "bug" }),
    Settings = Window:AddTab({ Title = "Configuration", Icon = "settings" })
}

-- Automation Tab
StatusLabel = Tabs.Automation:AddParagraph({
    Title = "Status: Idle",
    Content = "The script is currently awaiting instructions."
})

Tabs.Automation:AddSection("Heart Detector")
local HopperToggle = Tabs.Automation:AddToggle("HopperToggle", {Title = "Auto-Hop Loop", Default = false })

HopperToggle:OnChanged(function()
    if HopperToggle.Value then
        updateStatus("Hopper Toggle ON (Awaiting Start)")
        Fluent:Notify({Title = "Hopper Ready", Content = "Loop enabled. Press 'Start' to begin.", Duration = 3})
    else
        updateStatus("Idle")
    end
end)

Tabs.Automation:AddToggle("AutoTweenOnFind", {Title = "Auto-Tween on Discovery", Default = true })

Tabs.Automation:AddSection("Inventory Logic")
Tabs.Automation:AddSlider("MaxHeartLimit", {Title = "Max Hearts Allowed", Default = 1, Min = 1, Max = 10, Rounding = 1})
Tabs.Automation:AddToggle("TweenOnMax", {
    Title = "Tween on Max Hearts", 
    Default = false,
    Description = "If OFF: Skips heart and hops if inventory is full."
})

Tabs.Automation:AddButton({
    Title = "Start Hopper",
    Callback = function()
        if HopperToggle.Value then
            runHopperLogic()
        else
            updateStatus("Error: Toggle off")
            Fluent:Notify({Title = "Warning", Content = "Enable the 'Auto-Hop Loop' toggle first.", Duration = 3})
        end
    end
})

Tabs.Automation:AddSection("Manual Actions")
Tabs.Automation:AddButton({Title = "Check Server", Callback = function()
    local status, hp, obj = findDevilHeart()
    local colCheck = hp and checkPathCollision(hp)
    local res = status
    if status == "valid" and colCheck then res = "Bait (Collision)" end
    updateStatus("Manual Scan Result: " .. (res or "None"))
    updateDebugStats() -- Update debug tab info
end})

Tabs.Automation:AddButton({Title = "Tween to Heart", Callback = function() 
    local status, hp, obj = findDevilHeart() 
    if status == "valid" then startStagedTween(hp, obj) end 
end})

Tabs.Automation:AddButton({Title = "Stop Movement", Callback = function() stopTween(nil) end})

-- Debug Tab
Tabs.Debug:AddSection("Live Statistics")
SpeedLabel = Tabs.Debug:AddParagraph({
    Title = "Studs Per Second: 0",
    Content = "Tracking live movement speed."
})

HeartStatsLabel = Tabs.Debug:AddParagraph({
    Title = "Heart Stats: NO DATA",
    Content = "Scan for a heart to populate statistics."
})

Tabs.Debug:AddSection("Tools")
Tabs.Debug:AddButton({
    Title = "View Heart (Camera Toggle)",
    Description = "Attach camera to found heart.",
    Callback = toggleViewHeart
})

Tabs.Debug:AddButton({
    Title = "Highlight Heart",
    Description = "Toggle ESP outline on heart.",
    Callback = toggleHighlight
})

-- Configuration Tab
Tabs.Settings:AddSection("Server Hopping")
Tabs.Settings:AddSlider("ServerLogExpiry", {
    Title = "Server Block Duration",
    Description = "Time until a visited server is valid again.",
    Default = 30,
    Min = 5,
    Max = 120,
    Rounding = 1,
    Callback = function(Value) end
})

Tabs.Settings:AddSection("Movement Modes")
Tabs.Settings:AddToggle("SplitToggle", {Title = "Split XZ/Y", Default = false})

HeightLabel = Tabs.Settings:AddParagraph({
    Title = "Ground Offset: N/A",
    Content = "Enable Ground Only to see height status."
})

Tabs.Settings:AddToggle("GroundFollow", {Title = "Ground Only (Raycast)", Default = false})
Tabs.Settings:AddToggle("SegmentToggle", {Title = "Enable Segments", Default = false})

Tabs.Settings:AddSection("Speed Configuration")
Tabs.Settings:AddToggle("UseSTPS", {Title = "Use Exact STPS Speed", Default = true})
Tabs.Settings:AddInput("STPSValue", {Title = "Speed (STPS)", Default = "30", Numeric = true, Description = "Studs Per Second (Try 30-50)"})
Tabs.Settings:AddInput("TweenSpeed", {Title = "Legacy Speed", Default = "0.5", Numeric = true, Description = "Used if STPS is OFF"})

Tabs.Settings:AddSection("Segment Config")
Tabs.Settings:AddInput("TweenDist", {Title = "Segment Distance", Default = "50", Numeric = true})
Tabs.Settings:AddSlider("TweenInterval", {Title = "Wait Interval", Default = 1, Min = 0.1, Max = 10, Rounding = 1})
Tabs.Settings:AddDropdown("TweenEasing", {Title = "Easing", Values = {"Linear", "Quad", "Sine", "Cubic"}, Default = "Linear"})

Tabs.Settings:AddToggle("SegmentDrop", {
    Title = "Drop Between Segments", 
    Default = false, 
    Description = "If OFF: Player is anchored during waits. If ON: Player physics resume."
})

Tabs.Settings:AddSection("Discord Notifications")
Tabs.Settings:AddInput("WebhookURL", {Title = "URL", Default = "", Callback = function(v) webhookURL = v end})
Tabs.Settings:AddToggle("WebhookEnable", {Title = "Enable", Default = false }):OnChanged(function(v) webhookEnabled = v end)

-- [[ LOOPS ]]

RunService.Heartbeat:Connect(function()
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    
    if SpeedLabel and root then
        local velocity = root.Velocity
        local sps = math.floor(velocity.Magnitude + 0.5)
        SpeedLabel:SetTitle("Studs Per Second: " .. tostring(sps))
    end

    if HeightLabel and Fluent.Options.GroundFollow.Value and root then
        local rayOrigin = root.Position
        local rayDirection = Vector3.new(0, -100, 0)
        RayParamsExclude.FilterDescendantsInstances = {char}
        
        local res = workspace:Raycast(rayOrigin, rayDirection, RayParamsExclude)
        if res then
            local dist = math.floor((root.Position.Y - res.Position.Y) * 10) / 10
            HeightLabel:SetTitle("Height Offset: " .. tostring(dist) .. " studs")
        else
            HeightLabel:SetTitle("Height Offset: Void")
        end
    elseif HeightLabel then
         HeightLabel:SetTitle("Ground Offset: N/A")
    end
end)

SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)
SaveManager:IgnoreThemeSettings()
InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

Window:SelectTab(1)
updateStatus("Loading Config...")
SaveManager:LoadAutoloadConfig()
task.wait(0.5)
isInitialized = true

if HopperToggle.Value then
    updateStatus("Auto-Starting (Config)...")
    runHopperLogic()
else
    updateStatus("Idle")
end
