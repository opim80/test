-- [[ SINGLE INSTANCE & CLEANUP LOGIC ]]
if getgenv().CSMDH_Cleanup then
    pcall(function() getgenv().CSMDH_Cleanup() end)
end

-- Wait for Game & Player to load completely
if not game:IsLoaded() then game.Loaded:Wait() end
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

repeat task.wait() until LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

-- [[ UI & CONFIG LIBRARIES ]]
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

-- [[ STATE VARIABLES ]]
local killAuraActive = false
local killAllActive = false
local selectedMobs = {} 
local selectedWeapon = ""
local backupTweenSpeed = 65 
local vectorMethod = "vector.create(X, Y, Z)" -- Defaulted to the mathematically correct fix
local auraTask = nil
local evasionTask = nil
local currentTarget = nil 
local cameraTrackActive = false
local cameraTask = nil

local autoStatActive = false
local statTarget = "Melee"
local statAmount = 1

local autoDungeonActive = false
local autoRequestActive = false
local selectedDungeon = "DoubleDungeon"
local selectedDifficulty = "Easy"
local dungeonLoopTask = nil

local StatusDisplay = nil

-- Store new cleanup function globally for the NEXT time you execute
getgenv().CSMDH_Cleanup = function()
    if auraTask then task.cancel(auraTask) end
    if dungeonLoopTask then task.cancel(dungeonLoopTask) end
    if evasionTask then evasionTask:Disconnect() end
    if cameraTask then cameraTask:Disconnect() end
    
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.Velocity = Vector3.zero
        hrp.RotVelocity = Vector3.zero
    end
    
    local cam = workspace.CurrentCamera
    cam.CameraType = Enum.CameraType.Custom
    cam.CameraSubject = char and char:FindFirstChild("Humanoid")
    
    if getgenv().CSMDH_Window then
        pcall(function() getgenv().CSMDH_Window:Destroy() end)
    end
end

-- [[ HELPER FUNCTIONS ]]
local function updateStatus(msg)
    if StatusDisplay then
        StatusDisplay:SetDesc(msg)
    end
end

local function getBaseName(name)
    return string.gsub(name, "[_%s%d]+$", "")
end

local function isMobSelected(rawName, baseName)
    if killAllActive then return true end
    if type(selectedMobs) == "table" then
        if selectedMobs[baseName] or selectedMobs[rawName] then return true end
        if table.find(selectedMobs, baseName) or table.find(selectedMobs, rawName) then return true end
    end
    return false
end

local function getTargetPart(npc)
    return npc:FindFirstChild("HumanoidRootPart") or npc:FindFirstChild("Torso") or npc.PrimaryPart
end

local function getWeapons()
    local weapons = {}
    if LocalPlayer.Backpack then
        for _, tool in ipairs(LocalPlayer.Backpack:GetChildren()) do
            if tool:IsA("Tool") then table.insert(weapons, tool.Name) end
        end
    end
    if LocalPlayer.Character then
        for _, tool in ipairs(LocalPlayer.Character:GetChildren()) do
            if tool:IsA("Tool") then table.insert(weapons, tool.Name) end
        end
    end
    return weapons
end

local function getAvailableMobs()
    local mobs = {}
    local npcFolder = workspace:FindFirstChild("NPCs")
    if npcFolder then
        for _, npc in ipairs(npcFolder:GetChildren()) do
            local hum = npc:FindFirstChild("Humanoid")
            if hum and hum.Health > 0 then
                local baseName = getBaseName(npc.Name)
                if not table.find(mobs, baseName) then table.insert(mobs, baseName) end
            end
        end
    end
    return mobs
end

local function equipWeapon(weaponName)
    if not weaponName or weaponName == "" then return end
    local char = LocalPlayer.Character
    local backpack = LocalPlayer.Backpack
    if not char or not backpack then return end
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    local tool = backpack:FindFirstChild(weaponName)
    if tool and humanoid then humanoid:EquipTool(tool) end
end

-- [[ CAMERA TRACKING ]]
local function startCameraTracking()
    if cameraTask then cameraTask:Disconnect() end
    local camera = workspace.CurrentCamera
    
    cameraTask = RunService.RenderStepped:Connect(function()
        if cameraTrackActive then
            if currentTarget then
                local hum = currentTarget:FindFirstChild("Humanoid")
                if hum and camera.CameraSubject ~= hum then
                    camera.CameraSubject = hum
                end
            else
                local myHum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
                if myHum and camera.CameraSubject ~= myHum then
                    camera.CameraSubject = myHum
                end
            end
        end
    end)
end

local function stopCameraTracking()
    if cameraTask then cameraTask:Disconnect(); cameraTask = nil end
    local camera = workspace.CurrentCamera
    local myHum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
    if myHum then 
        camera.CameraSubject = myHum 
    end
end

-- [[ KILLAURA & MOVEMENT LOGIC ]]
local function startDynamicMovement()
    if evasionTask then evasionTask:Disconnect(); evasionTask = nil end

    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    evasionTask = RunService.Heartbeat:Connect(function(deltaTime)
        if not hrp or not hrp.Parent then return end
        
        hrp.Velocity = Vector3.zero
        hrp.RotVelocity = Vector3.zero

        local nextX = hrp.Position.X
        local nextY = hrp.Position.Y
        local nextZ = hrp.Position.Z

        if currentTarget then
            local targetPart = getTargetPart(currentTarget)
            
            if targetPart then
                local ray = Ray.new(hrp.Position, Vector3.new(0, -500, 0))
                local _, groundPos = workspace:FindPartOnRay(ray, char)
                local hoverHeight = groundPos and (groundPos.Y + 20) or hrp.Position.Y
                
                nextY = hrp.Position.Y + (hoverHeight - hrp.Position.Y) * 0.15
            else
                local tPos = currentTarget:GetPivot().Position
                local target3D = Vector3.new(tPos.X, tPos.Y + 20, tPos.Z)
                local direction = target3D - hrp.Position
                local dist3D = direction.Magnitude
                
                if dist3D > 1 then
                    local stepDist = backupTweenSpeed * deltaTime
                    if stepDist > dist3D then stepDist = dist3D end 
                    
                    local moveVector = direction.Unit * stepDist
                    nextX = hrp.Position.X + moveVector.X
                    nextY = hrp.Position.Y + moveVector.Y
                    nextZ = hrp.Position.Z + moveVector.Z
                end
            end
        else
            local ray = Ray.new(hrp.Position, Vector3.new(0, -500, 0))
            local _, groundPos = workspace:FindPartOnRay(ray, char)
            local hoverHeight = groundPos and (groundPos.Y + 20) or hrp.Position.Y
            
            nextY = hrp.Position.Y + (hoverHeight - hrp.Position.Y) * 0.15
        end

        hrp.CFrame = CFrame.new(nextX, nextY, nextZ)
    end)
end

local function startKillAura()
    if not killAuraActive then return end
    updateStatus("Initializing KillAura & Global Hover...")
    startDynamicMovement()

    if auraTask then task.cancel(auraTask); auraTask = nil end

    auraTask = task.spawn(function()
        local remote = ReplicatedStorage:WaitForChild("CombatSystem"):WaitForChild("Remotes"):WaitForChild("RequestHit")

        while killAuraActive do
            local npcFolder = workspace:FindFirstChild("NPCs")
            
            if not npcFolder then 
                updateStatus("Waiting for workspace.NPCs folder to load...")
                task.wait(0.5)
                continue 
            end

            updateStatus("Scanning for valid targets...")
            equipWeapon(selectedWeapon)
            currentTarget = nil
            
            local bestTarget = nil
            local closestDist = math.huge
            local charPos = LocalPlayer.Character and LocalPlayer.Character:GetPivot().Position or Vector3.zero

            local validMobFound = false

            for _, npc in ipairs(npcFolder:GetChildren()) do
                local hum = npc:FindFirstChild("Humanoid")
                
                if hum and hum.Health > 0 then
                    local baseName = getBaseName(npc.Name)
                    
                    if isMobSelected(npc.Name, baseName) then
                        validMobFound = true
                        local dist = (charPos - npc:GetPivot().Position).Magnitude
                        if dist < closestDist then
                            closestDist = dist
                            bestTarget = npc
                        end
                    end
                end
            end

            if not validMobFound then
                if not killAllActive and next(selectedMobs) == nil then
                    updateStatus("No mobs selected in dropdown and KillAll is OFF.")
                else
                    updateStatus("No selected mobs are currently alive.")
                end
            end

            currentTarget = bestTarget

            if currentTarget then
                local hum = currentTarget:FindFirstChild("Humanoid")

                while killAuraActive and currentTarget and currentTarget.Parent and hum and hum.Health > 0 do
                    local targetPart = getTargetPart(currentTarget)
                    
                    if targetPart then
                        local tPos = targetPart.Position
                        
                        -- The flexible vector logic based on your dropdown selection
                        local success, err = pcall(function() 
                            local args = {}
                            if vectorMethod == "vector.create(X, Y, Z)" then
                                args = { vector.create(tPos.X, tPos.Y, tPos.Z) }
                            elseif vectorMethod == "Vector3.new" then
                                args = { Vector3.new(tPos.X, tPos.Y, tPos.Z) }
                            elseif vectorMethod == "Raw Vector3" then
                                args = { tPos }
                            else
                                args = { vector.create(tPos) } -- The one that throws the error
                            end
                            
                            remote:FireServer(unpack(args)) 
                        end)
                        
                        -- Display the exact error on the status bar if it fails
                        if not success then
                            updateStatus("⚠️ REMOTE ERROR: " .. tostring(err))
                        else
                            local remotePath = remote:GetFullName()
                            updateStatus("Global Strike: " .. currentTarget.Name .. " | HP: " .. tostring(math.floor(hum.Health)) .. "\nMethod: " .. vectorMethod)
                        end
                    else
                        updateStatus("Target unrendered. Tweening to pivot: " .. currentTarget.Name)
                    end
                    
                    task.wait(0.35)
                end
                
                if currentTarget == nil or not currentTarget.Parent or (hum and hum.Health <= 0) then
                    updateStatus("Target defeated. Searching again...")
                    currentTarget = nil 
                end
            else
                task.wait(0.5) 
            end
        end
    end)
end

local function stopKillAura()
    killAuraActive = false
    currentTarget = nil
    updateStatus("KillAura Stopped. Idle.")
    if auraTask then task.cancel(auraTask); auraTask = nil end
    if evasionTask then evasionTask:Disconnect(); evasionTask = nil end
    
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.Velocity = Vector3.zero
        hrp.RotVelocity = Vector3.zero
    end
end

-- [[ STATS LOGIC ]]
local function startAutoStats()
    task.spawn(function()
        while autoStatActive do
            local statPointsFolder = LocalPlayer:FindFirstChild("Data") and LocalPlayer.Data:FindFirstChild("StatPoints")
            if statPointsFolder and tonumber(statPointsFolder.Value) >= statAmount then
                local args = { statTarget, statAmount }
                pcall(function() ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("AllocateStat"):FireServer(unpack(args)) end)
            end
            task.wait(1)
        end
    end)
end

-- [[ RAIDS LOGIC ]]
local function startAutoDungeon()
    if dungeonLoopTask then task.cancel(dungeonLoopTask); dungeonLoopTask = nil end

    dungeonLoopTask = task.spawn(function()
        while autoDungeonActive do
            local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
            local dungeonUI = playerGui and playerGui:FindFirstChild("DungeonUI")
            
            if dungeonUI then
                local replayFrame = dungeonUI:FindFirstChild("ReplayDungeonFrameVisibleOnlyWhenClearingDungeon")
                local contentFrame = dungeonUI:FindFirstChild("ContentFrame")
                
                if replayFrame and replayFrame.Visible then
                    local args = {"sponsor"}
                    pcall(function()
                        ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("DungeonWaveReplayVote"):FireServer(unpack(args))
                    end)
                elseif contentFrame and contentFrame.Visible then
                    pcall(function()
                        ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("DungeonWaveVote"):FireServer(selectedDifficulty)
                    end)
                    
                    if not killAuraActive then
                        killAllActive = true 
                        killAuraActive = true
                        startKillAura()
                    end
                elseif autoRequestActive then
                    pcall(function()
                        ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("RequestDungeonPortal"):FireServer(selectedDungeon)
                    end)
                end
            end
            task.wait(2)
        end
    end)
end

-- [[ UI INITIALIZATION ]]
local Window = Fluent:CreateWindow({
    Title = "Ultimate Hub",
    SubTitle = "V2.7.0 (Vector Override)",
    TabWidth = 140,
    Size = UDim2.fromOffset(420, 320),
    Acrylic = true, 
    Theme = "Dark"
})

getgenv().CSMDH_Window = Window

-- [[ MOBILE DROPDOWN PATCH ]]
task.spawn(function()
    local CoreGui = game:GetService("CoreGui")
    local function limitDropdownSize(obj)
        if obj:IsA("ScrollingFrame") then
            if obj.Name:lower():find("option") or (obj.Parent and obj.Parent.Name:lower():find("dropdown")) then
                if not obj:FindFirstChild("MobileListFix") then
                    local constraint = Instance.new("UISizeConstraint")
                    constraint.Name = "MobileListFix"
                    constraint.MaxSize = Vector2.new(math.huge, 140)
                    constraint.Parent = obj
                end
            end
        end
    end
    for _, desc in ipairs(CoreGui:GetDescendants()) do limitDropdownSize(desc) end
    CoreGui.DescendantAdded:Connect(limitDropdownSize)
end)

local Tabs = {
    Combat = Window:AddTab({ Title = "Combat", Icon = "swords" }),
    Stats = Window:AddTab({ Title = "Stats", Icon = "bar-chart" }),
    Raids = Window:AddTab({ Title = "Raids", Icon = "skull" }),
    Settings = Window:AddTab({ Title = "Settings", Icon = "settings" })
}

-- COMBAT TAB
StatusDisplay = Tabs.Combat:AddParagraph({
    Title = "Current Script Status",
    Content = "Idle. Awaiting execution."
})

Tabs.Combat:AddSection("Targeting Settings")
local MasterAuraToggle = Tabs.Combat:AddToggle("MasterAuraToggle", {Title = "Start KillAura", Default = false})
local KillAllToggle = Tabs.Combat:AddToggle("KillAllToggle", {Title = "Kill ALL Mobs (Overrides Selection)", Default = false})
KillAllToggle:OnChanged(function() killAllActive = KillAllToggle.Value end)

local MobDropdown = Tabs.Combat:AddDropdown("MobDropdown", {Title = "Select Mob Groups", Values = getAvailableMobs(), Multi = true, Default = {}})
MobDropdown:OnChanged(function(Value) selectedMobs = Value end)
Tabs.Combat:AddButton({Title = "Refresh Mob List", Callback = function() MobDropdown:SetValues(getAvailableMobs()) end})

Tabs.Combat:AddSection("Weapon & Extras")
local WeaponDropdown = Tabs.Combat:AddDropdown("WeaponDropdown", {Title = "Select Weapon", Values = getWeapons(), Multi = false, Default = ""})
WeaponDropdown:OnChanged(function(Value) selectedWeapon = Value end)
Tabs.Combat:AddButton({Title = "Refresh Weapons", Callback = function() WeaponDropdown:SetValues(getWeapons()) end})

-- New Dropdown to dynamically bypass "vector is nil" or "number expected" errors
Tabs.Combat:AddDropdown("VectorMethodDropdown", {
    Title = "Attack Vector Method", 
    Values = {"vector.create(X, Y, Z)", "Vector3.new", "Raw Vector3", "vector.create(Vector3) [Broken]"}, 
    Multi = false, 
    Default = "vector.create(X, Y, Z)"
}):OnChanged(function(Value) vectorMethod = Value end)

Tabs.Combat:AddSlider("TweenSpeedSlider", {
    Title = "Backup Tween Speed",
    Description = "Speed when moving to unrendered mobs",
    Default = 65,
    Min = 10,
    Max = 200,
    Rounding = 1
}):OnChanged(function(Value)
    backupTweenSpeed = Value
end)

local CamToggle = Tabs.Combat:AddToggle("CamToggle", {Title = "Spectate Target", Default = false})
CamToggle:OnChanged(function(Value)
    cameraTrackActive = Value
    if not cameraTrackActive then stopCameraTracking() else startCameraTracking() end
end)

MasterAuraToggle:OnChanged(function()
    if MasterAuraToggle.Value then
        killAuraActive = true
        startKillAura()
    else
        stopKillAura()
    end
end)

-- STATS TAB
Tabs.Stats:AddSection("Stat Information")
local StatDisplay = Tabs.Stats:AddParagraph({Title = "Available Stat Points", Content = "Calculating..."})
task.spawn(function()
    while task.wait(1) do
        local points = LocalPlayer:FindFirstChild("Data") and LocalPlayer.Data:FindFirstChild("StatPoints")
        if points then StatDisplay:SetDesc("Points: " .. tostring(points.Value)) end
    end
end)

Tabs.Stats:AddSection("Auto Allocation")
Tabs.Stats:AddDropdown("StatDropdown", {Title = "Target Stat", Values = {"Melee", "Defense", "Sword", "Power"}, Multi = false, Default = "Melee"}):OnChanged(function(Value) statTarget = Value end)
Tabs.Stats:AddInput("StatAmount", {Title = "Allocate Amount", Default = "1", Numeric = true}):OnChanged(function(Value) statAmount = tonumber(Value) or 1 end)
Tabs.Stats:AddToggle("AutoStatToggle", {Title = "Auto Allocate", Default = false}):OnChanged(function(Value)
    autoStatActive = Value
    if autoStatActive then startAutoStats() end
end)

-- RAIDS TAB
Tabs.Raids:AddSection("Dungeon Setup")
Tabs.Raids:AddDropdown("DungeonType", {Title = "Dungeon Type", Values = {"CidDungeon", "RuneDungeon", "DoubleDungeon"}, Multi = false, Default = "DoubleDungeon"}):OnChanged(function(Value) selectedDungeon = Value end)
Tabs.Raids:AddButton({Title = "Manual Request Portal", Callback = function() pcall(function() ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("RequestDungeonPortal"):FireServer(selectedDungeon) end) end})

Tabs.Raids:AddSection("Auto Raid Loop")
Tabs.Raids:AddDropdown("DifficultyType", {Title = "Difficulty", Values = {"Easy", "Medium", "Hard", "Extreme"}, Multi = false, Default = "Easy"}):OnChanged(function(Value) selectedDifficulty = Value end)

local AutoRequestToggle = Tabs.Raids:AddToggle("AutoRequestToggle", {Title = "Auto-Request Portal (If outside)", Default = false})
AutoRequestToggle:OnChanged(function(Value) autoRequestActive = Value end)

Tabs.Raids:AddToggle("AutoDungeonToggle", {Title = "Auto Dungeon (Vote & Replay)", Default = false}):OnChanged(function(Value)
    autoDungeonActive = Value
    if autoDungeonActive then 
        startAutoDungeon() 
    elseif dungeonLoopTask then
        task.cancel(dungeonLoopTask)
        dungeonLoopTask = nil
    end
end)

-- [[ CONFIG & SETTINGS LOGIC ]]
SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({})
SaveManager:SetFolder("UltimateHub")
InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

Window:SelectTab(1)
SaveManager:LoadAutoloadConfig()
